{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Meeting Transcriber main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
